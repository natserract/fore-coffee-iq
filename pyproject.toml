[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "simple-chatbot"
version = "0.1.0"
description = ""
authors = ["Natserract <natserract@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.11"

[tool.pyright]
venvPath = "."
venv = ".venv"
